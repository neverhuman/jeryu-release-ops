[package]
name = "jeryu-git-guard"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deny-by-default git command allowlist for confined coding agents: only branch-local activity on the assigned branch is permitted; network, worktree, and branch-mutation operations are refused with typed repair guidance"

[lib]
name = "jeryu_git_guard"
path = "src/lib.rs"

[[bin]]
name = "jeryu-git"
path = "src/bin/jeryu-git.rs"

[dependencies]

[lints]
workspace = true
