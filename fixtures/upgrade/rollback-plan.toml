from = "0.9.0"
to = "0.10.0"
preflight = ["backup-complete", "audit-chain-valid", "slo-green"]
rollback = ["stop-api-writes", "restore-schema-snapshot", "replay-audit-outbox", "resume-api"]
invariants = ["no-receipt-loss", "tenant-boundaries-intact", "sso-configs-valid"]
